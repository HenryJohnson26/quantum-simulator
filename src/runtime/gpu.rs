pub fn run_on_gpu(amplitudes: &mut [f64], gate_matrix: &[[f64; 2]; 2]) {
    // TODO: encode this for wgpu/cuda
    // For now, stub this out for CPU until GPU acceleration added
    println!("GPU acceleration not yet implemented, running on CPU");
}
